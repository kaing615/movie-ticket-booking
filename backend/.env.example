PORT=4000 # Port mà backend sẽ chạy
MONGO_URL=mongodb://mongo:27017/ # Đường dẫn kết nối MongoDB, cần cài Docker Desktop trước
JWT_SECRET= # Điền chuỗi bất kỳ, dùng để ký JWT


# Khi chạy thực tế:
# Tạo file .env (copy y nguyên các biến từ .env.example).

# Bỏ phần chú thích (mọi thứ sau dấu #), và điền giá trị thật cho JWT_SECRET.

# Giữ nguyên PORT và MONGO_URL nếu đang chạy MongoDB qua Docker.

## Hướng dẫn cấu hình biến môi trường

# 1. Copy file `.env.example` thành `.env`
# 2. Bỏ hết phần ghi chú sau dấu #
# 3. Điền giá trị thật cho biến `JWT_SECRET`
# 4. Giữ nguyên các biến còn lại nếu chạy MongoDB qua Docker Compose